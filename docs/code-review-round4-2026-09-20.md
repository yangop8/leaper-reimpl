# Leaper 第四轮修复验收（2026-09-20）

验收提交：`500fe35`。对照第三轮报告 `docs/code-review-round3-2026-09-20.md`，聚焦最后剩余的 SST 空预测范围预算漏洞，并检查相关回归。继续按研究原型尺度检视。

**结论：通过。上一轮唯一剩余项已修复；此前检视提出的修复要求可以全部关闭。本轮未发现需要继续修改的阻塞问题。**

## 修复核对

`adapters/rocksdb/leaper_rocksdb.cc` 中，SST 暖缓存现在在每次 Seek 前检查额度，并在 Seek 后无条件检查额度，不依赖迭代器是否有效或结果是否落在预测范围内；逐键扫描中的检查也保留。额度耗尽后退出范围及文件循环，因此空预测范围无法继续绕过预算。

此前 End 与 Begin 使用同一适配器锁的修复仍然保留，本次差异没有改动该同步逻辑。独立复核也确认上述控制流与新增测试确实覆盖旧问题。

## 实际验证

独立探针使用本次提交的适配器及核心源码重新编译，直接通过公开 listener 回调处理真实 SST；没有修改实现或插入测试逻辑。

| 用例 | 预算 | 实际计数 | 结果 |
|---|---:|---:|---|
| SST 只有奇数范围有键，预测全部偶数范围；12,000 条记录，范围大小 1,000 | 32 | 32 | 通过；修复前同一探针计数 355 |
| SST 连续范围，范围大小 40,000 | 32 | 32 | 通过 |
| SST 连续范围，范围大小 1,000 | 4 | 4 | 通过 |
| iterator 连续范围，范围大小 40,000 | 32 | 32 | 通过 |
| iterator 连续范围，范围大小 1,000 | 4 | 4 | 通过 |

数值沿用适配器现有 warmed_blocks 计数口径。所有 SST 探针均打开 1 个文件，打开失败数为 0。

仓库新增 `budget_check` 实测通过：无预算空范围对照计数 355；32 块预算下，空范围和有键范围均计数 32，均记录一次预算停止。该测试使用真实 SST，且要求无预算对照至少读取 100 块，能避免缓存命中使预算验证失去意义。

`core_check`、`mapper_check` 均通过。执行命令：

```sh
cmake --build build --target budget_check core_check mapper_check -j 4
./build/budget_check /Users/yang/Documents/Codex/2026-09-19/ji/work/leaper-round4-budget
./build/leaper/core_check
./build/leaper/mapper_check
```

## 验收边界

本轮未重跑完整实验矩阵、模型训练或受控并发实验；此前通过的实验表格与其他修复结论沿用前轮报告，并核对本次差异未回退相关修复。通过表示此前列出的明显 bug 和不合理设计项已完成修复，不表示对所有输入与性能结论作穷尽证明。

本轮仅新增此报告，未修改实现、模型或实验数据。无需为关闭此前检视项继续返工。
